server:
  port: 8092
spring:
  application:
    name: rating-service
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8787/eureka}
  instance:
    preferIpAddress: true